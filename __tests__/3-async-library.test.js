test.todo('a test suite for section 3-async-library');

const mapAsync = require('../challenges/3-async-library');

describe('', () => {
    test('', () => {
        const input = undefined;

        const output = funcName(input);

        const expected = undefined;
        expect(output).to(expected);
    });
});
